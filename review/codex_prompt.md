You are auditing the measurement and statistics code of a research study before an arXiv preprint. Be adversarial; find bugs that would change a reported number or conclusion. Do not edit any files. Do not call the network API (every response is cached under data/cache; harness/jev.py with offline=True never calls it).

Repository: current directory. Paper: paper/main.tex. Plan: experiments/PLAN.md.
Code to audit: experiments/analyze_e1e3.py (E1 coupling asymmetry, bootstrap SE, coherent null, total-probability residuals; E3 AUROC/Brier/ECE, LOO logistic baselines), experiments/e2_hamiltonian.py + e2b_convention.py (Ising energies, config labelling, Spearman, H2 Jordan-Wigner weights, score->energy mapping), experiments/e4_selected_ci.py + analyze_e4.py (CAS determinant Hamiltonian build, spin penalty, subspace energies, ranking baselines, tie-breaking, area metric, Wilcoxon), experiments/figures.py, experiments/qc_labels.py (labels).

For each numeric claim in paper/main.tex, check the code that produces it. You may run the analysis scripts with .venv/bin/python (they read cached JSON; e4_selected_ci.py and qc_labels.py recompute PySCF but are cache-only for the API). Specifically check: sign conventions (J_ab definition, E=-log p, Ising energy vs labels, rho(p,-E)); whether the bootstrap/null is valid; ECE implementation; whether the 'score' field is an expected level index (so E_read = lv0 + score*step); qubit ordering for H2 (is |1100> the HF state?); whether subspace energies with the spin penalty are correct; whether the random/Jev curves average correctly; any off-by-one in k; any data leakage in LOO.

Write your findings to review/codex_review.md: a numbered list, each with file:line, what is wrong, the concrete effect on a reported number, severity (blocking/major/minor), and how you verified it. If a claim checks out, list it briefly under 'Verified'.
